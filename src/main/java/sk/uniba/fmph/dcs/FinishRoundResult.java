package sk.uniba.fmph.dcs;

enum FinishRoundResult {
    NORMAL,
    GAME_FINISHED;

    @Override
    public String toString() {
        switch(this) {
            return super.toString();
        }
    }
}